<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.apitester.ttalkkag.mapper.DatasetMapper">
    <!-- 데이터셋 전체 조회 -->
    <select id="findAllDatasets" resultMap="DatasetResultMap">
        SELECT id, name, description, created_at
        FROM dataset
    </select>

    <!-- 데이터셋 ID로 조회 -->
    <select id="findDatasetById" resultMap="DatasetResultMap">
        SELECT id, name, description, created_at
        FROM dataset
        WHERE id = #{id}
    </select>

    <!-- 데이터셋 삽입 -->
    <insert id="insertDataset" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dataset (name, description, created_at)
        VALUES (#{name}, #{description}, NOW())
    </insert>

    <!-- 데이터셋 삭제 -->
    <delete id="deleteDataset">
        DELETE FROM dataset WHERE id = #{id}
    </delete>

    <!-- 데이터셋 수정 -->
    <update id="updateDataset" parameterType="Dataset">
        UPDATE dataset
        SET name = #{name}, description = #{description}
        WHERE id = #{id}
    </update>

    <!-- 데이터셋에 포함된 변수들 조회 -->
    <select id="findVariablesByDatasetId" resultType="DatasetVariable">
        SELECT id AS id,
        dataset_id AS datasetId,
        name AS name,
        type AS type,
        description AS description,
        created_at AS createdAt
        FROM dataset_variable
        WHERE dataset_id = #{datasetId}
    </select>

    <!-- 데이터셋 검색 -->
    <select id="searchDatasets" parameterType="String" resultMap="DatasetResultMap">
        SELECT *
        FROM dataset
        WHERE name LIKE CONCAT('%', #{query}, '%')
        OR description LIKE CONCAT('%', #{query}, '%')
    </select>

    <!-- 데이터셋 ResultMap -->
    <resultMap id="DatasetResultMap" type="Dataset">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="description" column="description" />
        <result property="createdAt" column="created_at" />
        <collection property="variables" ofType="DatasetVariable"
                    column="id" select="findVariablesByDatasetId" />
    </resultMap>

</mapper>
