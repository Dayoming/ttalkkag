<configuration>
    <!-- 콘솔 로그 출력 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 파일 로그 출력 (일별 로테이션) -->
    <appender name="APPLICATION_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 로그 파일 경로 -->
        <file>logs/application.log</file>

        <!-- 롤링 정책: 일별로 로그 파일 생성 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 파일 이름 형식 (일별로 로그 파일 생성) -->
            <fileNamePattern>logs/application.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 로그 파일 최대 보관 기간: 30일 -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- TLO 로그 파일 출력 (일별 로테이션) -->
    <appender name="TLO_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/tlo.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/tlo.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <!-- CALL 로그 파일 출력 (일별 로테이션) -->
    <appender name="CALL_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/call.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/call.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <!--  TRANSACTION 로그 파일 출력 (일별 로테이션) -->
    <appender name="TRANSACTION_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/transaction.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/transaction.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- WebSocket 관련 로그 -->
    <logger name="com.apitester.ttalkkag.log.WebSocketEventListener" level="INFO" additivity="false">
        <appender-ref ref="APPLICATION_LOG" />
    </logger>

    <!-- DiskSpaceMonitor 관련 로그 -->
    <logger name="com.apitester.ttalkkag.log.DiskSpaceMonitor" level="INFO" additivity="false">
        <appender-ref ref="APPLICATION_LOG" />
    </logger>

    <!-- TLO 로그 설정 -->
    <logger name="com.apitester.ttalkkag.log.TLO" level="INFO" additivity="false">
        <appender-ref ref="TLO_LOG" />
    </logger>

    <!-- CALL 로그 설정 -->
    <logger name="com.apitester.ttalkkag.log.CALL" level="INFO" additivity="false">
        <appender-ref ref="CALL_LOG" />
    </logger>

    <!-- TRANSACTION 로그 설정 -->
    <logger name="com.apitester.ttalkkag.log.TRANSACTION" level="INFO" additivity="false">
        <appender-ref ref="TRANSACTION_LOG" />
    </logger>

    <!-- 루트 로거 설정 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="APPLICATION_LOG" />
    </root>
</configuration>
